# Media Overseerr

Request management frontend for Plex + *arr stack.

## Notes
- Uses SQLite (default)
- Connects to external `media-network`
- Integrates with Sonarr / Radarr

## Usage
```bash
cp .env.example .env
docker compose up -d
